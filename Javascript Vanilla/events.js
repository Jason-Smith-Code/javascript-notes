// Events are the triggers

// an event attribute can be applied to an element in html
// eventName={}

// onclick={}  when the html element is clicked
// onchange={}  when the html element has changed
// onmouseover={}   when the user hovers over the html element
// onmouseout={}    when the user has hovered over the html element, but then leaves the hover area
// onkeydown={} when a key is pressed
// onload={}    when the browser has finished loading the page
// https://www.w3schools.com/jsref/dom_obj_event.asp
