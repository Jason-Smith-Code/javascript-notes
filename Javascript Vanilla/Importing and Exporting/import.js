// import : after importing, we can then use that with dot notation to access functions within the exorted module
// directory must be initialized before this can work

const exportedFileName = require("./export");

exportedFileName.functionName();
